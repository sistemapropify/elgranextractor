# Azure App Service configuration
# Forces Python platform detection
platform: python
platform_version: '3.12'
# Startup command delegates to startup.sh which handles ODBC driver install,
# static files, migrations, and gunicorn with optimized settings
startup_command: "bash /home/site/wwwroot/startup.sh"
