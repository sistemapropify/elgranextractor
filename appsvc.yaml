# Azure App Service configuration
# Forces Python platform detection
platform: python
platform_version: '3.12'
startup_command: "export PYTHONPATH=$(pwd):$(pwd)/webapp && python webapp/manage.py collectstatic --noinput && gunicorn --bind=0.0.0.0 --timeout 600 --access-logfile '-' --error-logfile '-' webapp.wsgi:application"