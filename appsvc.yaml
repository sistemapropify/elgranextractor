# Azure App Service configuration
# Forces Python platform detection
platform: python
platform_version: '3.12'
startup_command: "cd /home/site/wwwroot && python -m webapp.manage collectstatic --noinput && gunicorn --bind=0.0.0.0 --timeout 600 --access-logfile '-' --error-logfile '-' webapp.wsgi:application"