# Azure App Service configuration
platform: python
platform_version: '3.12'
# Startup command debe estar VACIO en Azure Portal
# para que Oryx use el Procfile automaticamente.
# NO usar startup_command aqui - Oryx extrae a /tmp/8dee*/
# y no encuentra los archivos en /home/site/wwwroot/
