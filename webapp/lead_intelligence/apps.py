from django.apps import AppConfig


class LeadIntelligenceConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "lead_intelligence"
    verbose_name = "Inteligencia gerencial de leads"

