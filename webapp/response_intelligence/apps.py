from django.apps import AppConfig


class ResponseIntelligenceConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "response_intelligence"
    verbose_name = "Motor de respuestas IA (few-shot + RAG)"
