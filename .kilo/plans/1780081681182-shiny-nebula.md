# Plan: Sistema de Seguimiento de Propuestas WhatsApp

## 1. Nuevo Modelo `PropuestaWhatsApp` en `webapp/matching/models.py`

```python
class PropuestaWhatsApp(models.Model):
    class Status(models.TextChoices):
        ENVIADA = 'enviada', 'Enviada'
        RESPONDIDA = 'respondida', 'Respondida'
        INTERESADO = 'interesado', 'Interesado'
        NO_INTERESADO = 'no_interesado', 'No interesado'
        VISITA_AGENDADA = 'visita_agendada', 'Visita agendada'
        CERRADA = 'cerrada', 'Cerrada'

    requerimiento = models.ForeignKey(Requerimiento, on_delete=models.CASCADE, db_constraint=False)
    propiedad_id = models.BigIntegerField(verbose_name='ID de propiedad en dbpropify_be')
    propiedad_code = models.CharField(max_length=50, blank=True)
    propiedad_title = models.CharField(max_length=500, blank=True)
    propiedad_price = models.DecimalField(max_digits=15, decimal_places=2, null=True, blank=True)
    propiedad_currency_id = models.BigIntegerField(null=True, blank=True)
    propiedad_district_id = models.BigIntegerField(null=True, blank=True)
    agente_nombre = models.CharField(max_length=200, blank=True)
    agente_telefono = models.CharField(max_length=20, blank=True)
    mensaje_enviado = models.TextField(blank=True)
    status = models.CharField(max_length=20, choices=Status.choices, default=Status.ENVIADA)
    enviado_en = models.DateTimeField(auto_now_add=True)
    respondido_en = models.DateTimeField(null=True, blank=True)
    notas = models.TextField(blank=True)

    class Meta:
        db_table = 'matching_propuestawhatsapp'
        ordering = ['-enviado_en']
```

## 2. Endpoint API para guardar propuesta

En `webapp/matching/views.py`, agregar un nuevo ViewSet action:

```python
class PropuestaWhatsAppViewSet(viewsets.ViewSet):
    @action(detail=False, methods=['POST'])
    def guardar(self, request):
        # Recibe: requerimiento_id, propiedad_dict (del resultado del matching), agente_nombre, agente_telefono, mensaje
        # Crea PropuestaWhatsApp
        # Retorna { success: true, id: ... }
```

En `webapp/matching/urls.py`, agregar ruta:

```python
path('api/propuesta/guardar/', views.PropuestaWhatsAppViewSet.as_view({'post': 'guardar'}), name='propuesta-guardar'),
```

## 3. Modificar `enviarPropuestaWhatsApp()` en `calendar.html`

Actualmente la función abre WhatsApp y no guarda nada. Se modificará para:

1. Hacer `fetch POST` a `/matching/api/propuesta/guardar/` con los datos del `ultimoRequerimientoData` y `ultimaPropiedadTop`
2. Luego de la respuesta exitosa, abrir WhatsApp normalmente

## 4. Pipeline en template del agente

### 4.1 Vista en `webapp/agentes/views.py`

```python
class AgentePropuestasView(DetailView):
    model = Agente
    template_name = 'agentes/pipeline_propuestas.html'
    
    def get_context_data(self, **kwargs):
        context = super().get_context_data(**kwargs)
        # Buscar propuestas por nombre de agente o teléfono
        propuestas = PropuestaWhatsApp.objects.filter(
            Q(agente_nombre__icontains=self.object.nombre_completo) |
            Q(agente_telefono=self.object.telefono)
        ).order_by('-enviado_en')
        context['propuestas'] = propuestas
        # Estadísticas por status
        context['status_counts'] = propuestas.values('status').annotate(total=Count('id'))
        return context
```

### 4.2 URL en `webapp/agentes/urls.py`

```python
path('<int:pk>/propuestas/', views.AgentePropuestasView.as_view(), name='agente-propuestas'),
```

### 4.3 Template `webapp/agentes/templates/agentes/pipeline_propuestas.html`

- Tabla con columnas: Fecha envío, Requerimiento ID, Propiedad (código + título), Precio, Status, Acciones
- Barras de filtro por status (Enviada, Respondida, Interesado, etc.)
- Botón para cambiar status (POST a endpoint de actualización)

### 4.4 Endpoint para cambiar status

```python
@action(detail=True, methods=['POST'])
def actualizar_status(self, request, pk=None):
    # Recibe nuevo status
    # Actualiza PropuestaWhatsApp
```

Ruta:
```python
path('api/propuesta/<int:pk>/status/', views.PropuestaWhatsAppViewSet.as_view({'post': 'actualizar_status'}), name='propuesta-status'),
```

### 4.5 Enlace en lista de agentes

Agregar en `lista_agentes.html` un botón "Propuestas" en la columna de acciones:

```html
<a href="{% url 'agentes:agente-propuestas' agente.id %}" class="action-btn" title="Propuestas WhatsApp">
    <i class="fab fa-whatsapp"></i>
</a>
```

## 5. Migración

```bash
python manage.py makemigrations matching
python manage.py migrate
```

## Archivos a modificar

| Archivo | Acción |
|---------|--------|
| `webapp/matching/models.py` | Agregar modelo `PropuestaWhatsApp` |
| `webapp/matching/views.py` | Agregar `PropuestaWhatsAppViewSet` con actions `guardar` y `actualizar_status` |
| `webapp/matching/urls.py` | Agregar rutas de propuesta |
| `webapp/matching/serializers.py` | Agregar `PropuestaWhatsAppSerializer` |
| `webapp/matching/templates/matching/calendar.html` | Modificar `enviarPropuestaWhatsApp()` para guardar via API |
| `webapp/agentes/views.py` | Agregar `AgentePropuestasView` |
| `webapp/agentes/urls.py` | Agregar ruta `propuestas/` |
| `webapp/agentes/templates/agentes/lista_agentes.html` | Agregar botón "Propuestas" en acciones |
| `webapp/agentes/templates/agentes/pipeline_propuestas.html` | **NUEVO** - Template del pipeline |

## Notas

- El modelo usa `db_constraint=False` porque `propiedad_id` es un ID de la BD externa `dbpropify_be`, no un FK de Django.
- La propuesta se asocia al agente por nombre y teléfono (no por FK directa), porque el agente en `Requerimiento` viene como texto libre.
- Status inicial siempre `ENVIADA`.
