[config]
SCM_DO_BUILD_DURING_DEPLOYMENT = true
PYTHON_VERSION = 3.12
# The startup.sh script handles ODBC driver, collectstatic, migrations, and gunicorn
# See startup.sh for the full startup sequence
